name: secrets

on:
  workflow_dispatch:

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
#      - uses: j5pu/actions/secrets@main
#        with:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          repository: mnopi/secrets

      - name: organization secrets
        uses: actions/github-script@v5
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: "mnopi",
              repo: "secrets",
              workflow_id: "main.yaml",
              ref: "main"
            });

#      - uses: j5pu/actions/secrets@main

      - shell: bash
        run: |
           [ ! "${TWINE_PASSWORD-}" ] || echo defined
           echo ${TWINE_USER}
